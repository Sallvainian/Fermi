# Fermi macOS App Configuration
app_identifier("com.academic-tools.fermi")
apple_id(ENV["APPLE_ID"] || "")
itc_team_id(ENV["APPLE_TEAM_ID"] || "W778837A9L")
team_id("W778837A9L")

# For macOS apps
for_platform :mac do
  app_identifier "com.academic-tools.fermi"
end