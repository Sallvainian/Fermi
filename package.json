{
  "name": "fermi",
  "version": "0.9.6",
  "description": "Flutter education management platform",
  "scripts": {
    "build": "echo 'Skipping Flutter build in Cloudflare environment'",
    "deploy": "echo 'Deploy handled by Cloudflare Pages'"
  },
  "devDependencies": {
    "codebase-map": "^0.6.0",
    "wrangler": "^4.38.0"
  },
  "dependencies": {
    "mcp-firestore": "file:../mcp-firestore-fixed"
  }
}
